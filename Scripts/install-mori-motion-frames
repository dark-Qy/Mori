#!/bin/bash

set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
readonly REPO_ROOT
DEFAULT_PYTHON="/Users/shubing/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3"

if [[ -n "${MORI_MOTION_PYTHON:-}" ]]; then
  PYTHON="$MORI_MOTION_PYTHON"
elif [[ -x "$DEFAULT_PYTHON" ]]; then
  PYTHON="$DEFAULT_PYTHON"
else
  PYTHON="$(command -v python3)"
fi
readonly PYTHON

exec "$PYTHON" "$REPO_ROOT/Tools/MoriMotion/install_product_motion.py" \
  --repo-root "$REPO_ROOT" "$@"
